# 20251118
### 1. Allow overwrite
apt command:      

```
sudo apt install --allow-downgrades --allow-remove-essential --allow-change-held-packages -o Dpkg::Options::="--force-overwrite" --force-yes  xxx or ./adbc.deb
```
### 2. mdbook-mermaid
Install via:     

```
sudo pacman -S mdbook mdbook-mermaid
cd ~/book_directory
 mdbook-mermaid install `pwd`
```
