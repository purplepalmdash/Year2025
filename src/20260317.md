# 20260317
### 1. restart cuttlefish
command:      

```
HOME=$PWD ./bin/launch_cvd \
  --daemon \
  --start_webrtc=true \
  --num_instances=1 \
  --cpus=8 \
  --memory_mb=8192
```

### 2. rebuild redroid13
Code preparation:    

```
mkdir redroid13
cd redroid13/
export REPO_URL='https://mirrors.bfsu.edu.cn/git/git-repo'
repo init --depth 1 -u https://mirrors.bfsu.edu.cn/git/AOSP/platform/manifest -b android-13.0.0_r82
repo sync
git -c http.proxy=socks5h://192.168.1.213:14389 clone https://github.com/remote-android/local_manifests.git  .repo/local_manifests -b 13.0.0
# redsocks enable
HTTP_PROXY=socks5h:://192.168.1.213:14389 HTTPS_PROXY=socks5h://192.168.1.213:14389 repo sync -j1 --force-sync
repo forall -c "git lfs pull"
/media/sda/redroid/redroid-patches/apply-patch.sh /media/sda/redroid/redroid13/
```
Build via:      

```
sudo docker run -it --hostname redroid13 --name redroid13 -v `pwd`:/src redroid-builder
cd /src
source build/envsetup.sh
lunch
m -j12 dist
```
