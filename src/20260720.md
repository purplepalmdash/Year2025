# 20260720
### 1. Build waydroid-atv docker image(failed)
Host:   

```
mkdir waydroid_atv_lineageos23.2
cd waydroid_atv_lineageos23
cp -r ../jun29_232/lineage23.2/.repo/ .
export REPO_URL='https://mirrors.bfsu.edu.cn/git/git-repo'
 repo init -u https://mirrors.bfsu.edu.cn/git/lineageOS/LineageOS/android.git -b lineage-23.2 --git-lfs
repo sync trusty/vendor/google/aosp
repo sync -l build/make
```
Change to crossgfw environment(fedora):     

```
cd waydroid_atv_lineageos23.2/
wget -O - https://raw.githubusercontent.com/WayDroid-ATV/android_vendor_waydroid/lineage-23.2/manifest_scripts/generate-manifest.sh | bash
export REPO_URL='https://mirrors.bfsu.edu.cn/git/git-repo'
 repo sync -j1 --fail-fast --force-sync
repo sync -j8 --fail-fast
```
Build with :      

```
  sudo docker run -it --name build_wayd-v `pwd`:/mnt/lineage  -v /home/dash/ccache16:/ccache waydroid-build-24.04:aosp16build /bin/bashbin/bash
root@beaf8f7303a9:/# cd /mnt/lineage


mv external/mesa external/mesa.bak
build/make/core/base_rules.mk:320: error: external/mesa3d/android: MODULE.TARGET.SHARED_LIBRARIES.meson.dummy.64 already defined by external/mesa.bak/android.
rm -rf external/mesa.bak
root@beaf8f7303a9:/mnt/lineage# time make systemimage -j$(nproc --all) && time make vendorimage -j$(nproc --all)
build/make/core/base_rules.mk:320: error: external/minigbm/gbm_mesa_driver: MODULE.TARGET.SHARED_LIBRARIES.libgbm_mesa_wrapper already defined by external/minigbm-upstream/gbm_mesa_driver.

root@beaf8f7303a9:/mnt/lineage# mv external/minigbm-upstream/ ~
mv hardware/mainline/common/libhardware/gralloc/gbm/ ~
```
### 2. sync from 0
sync via:    

```
root@fedora:/media/nvme/RE_waydroid_atv_lineageos23.2# repo sync
Syncing: 63% (749/1186) 44:40 | 4 jobs | 0:43 kernel/prebuilts/6.12/arm64 @ kernel/prebuilts/6.12/arm64

```
