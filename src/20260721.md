# 20260721
### 1. fast grep
via ripgrep:     

```
rg "redroid" -g '!out/' -g '!.repo/'
       rg - recursively search the current directory for lines matching a pattern

```
### 2. find the latest modified files
via:     

```
$ find . -type d \( -name "out" -o -name ".repo" \) -prune -o -type f -printf '%T@ %p\n' | sort -nr | head -n 100 | cut -d' ' -f2-
./device/virt/virtio-common/services/virtgpu_detect/virtgpu_detect.c
./device/virt/virtio_x86_64/device.mk
./device/virt/virt-common/overlays/rro_overlays/LowPerformanceSettingsProviderOverlay/res/values/defaults.xml
./device/virt/virtio_x86_64/BoardConfig.mk
./device/virt/virt-common/overlays/overlay/frameworks/base/packages/SettingsProvider/res/values/defaults.xml
./build/make/target/product/gsi/Android.bp
```
### 3. build waydroid image 
Via:    

```
sudo mount system.img system -o ro && sudo mount vendor.img vendor -o ro && sudo tar --xattrs -c vendor -C system --exclude="./vendor" . | sudo docker import --platform=linux/amd64 -c 'ENTRYPOINT ["/init", "ro.hardware.hwcomposer=redroid", "ro.adb.secure=0"]' - ownwaydroid232jul21 && sudo umount system && sudo umount vendor
```
