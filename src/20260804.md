# 20260804
### 1. fedora repo(42)
via:     

```
sudo sed -i -r -e "
/^metalink=/s/^/#/
/^#*baseurl=/{s/^#//
s%download.(fedoraproject.org|example)\
%archives.fedoraproject.org%
s%(/pub)(/fedora/)%\1/archive\2%}
" /etc/yum.repos.d/fedora*.repo
```
### 2. debian trxie build items
Get src:    

```
mkdir ~/debian-build && cd ~/debian-build
apt source libvirglrenderer1
apt source qemu-system-x86
sudo apt build-dep -y libvirglrenderer1 qemu-system-x86
```
modification:      

```
cd virglrenderer-1.1.0/
cat meson_options.txt | grep -i video -A3 -B3
)

option(
  'video',
  type : 'boolean',
  value : 'true',
  description : 'enable support for hardware video acceleration'
)

```
build via:     

```
dpkg-buildpackage -us -uc -b -j$(nproc)
```
Verification:     

```
test@debian:~/debian-build/virglrenderer-1.1.0$ cat ./obj-x86_64-linux-gnu/meson-logs/meson-log.txt | grep -i video
Build Options: -Dpython.bytecompile=-1 -Dtests=true -Dvideo=true -Drender-server=true -Drender-server-worker=process -Dvenus=true -Dprefix=/usr -Dlibdir=lib/x86_64-linux-gnu -Dlocalstatedir=/var -Dsysconfdir=/etc -Dbuildtype=plain -Dwrap_mode=nodownload
    video                     : true
    video                     : true
test@debian:~/debian-build/virglrenderer-1.1.0$ ls ../*.deb
../libvirglrenderer1_1.1.0-2_amd64.deb         ../virgl-server_1.1.0-2_amd64.deb
../libvirglrenderer1-dbgsym_1.1.0-2_amd64.deb  ../virgl-server-dbgsym_1.1.0-2_amd64.deb
../libvirglrenderer-dev_1.1.0-2_amd64.deb
```
