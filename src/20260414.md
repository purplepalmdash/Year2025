# 20260414
### 1. waydroid(arm64) working tips
Steps:    

```
sudo apt update -y
sudo apt install -y lightdm
# select lightdm as default wm
sudo reboot
```
Choose wayland sessions:      

```
echo $XDG_SESSION_TYPE
wayland
```
Install waydroid:      

```
curl -s https://repo.waydro.id | sudo bash
sudo apt-get install -y waydroid
sudo waydroid init
```
### 2. hermes(ubuntu2204)
Install steps:      

```
sudo apt install -y curl git build-essential
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
```
Select provider:     

```
Custom endpoint
API base URL [e.g. https://api.example.com/v1]: http://192.168.1.165:8080/v1
API base URL [e.g. https://api.example.com/v1]: http://192.168.1.165:8080/v1
API key [optional]: 
Verified endpoint via http://192.168.1.165:8080/v1/models (1 model(s) visible)
  Detected model: Qwen3.5-35B-A3B-Uncensored-HauhauCS-Aggressive-Q8_0.gguf
  Use this model? [Y/n]: y
Context length in tokens [leave blank for auto-detect]: 
Display name [192.168.1.165:8080]: horsepower

Platform for Weixin(WeChat):    

  How should direct messages be authorized?
  ↑↓ navigate  ENTER/SPACE select  ESC cancel

 → (●) Use DM pairing approval (recommended)
   (○) Allow all direct messages
   (○) Only allow listed user IDs
   (○) Disable direct messages

```

