# 20260728
### 1. Continue building
Run:   

```
$ANDROID_BUILD_TOP/development/vndk/tools/header-checker/utils/create_reference_dumps.py -products lineage_waydroid_x86_64
```
When building libva/libvautils, will encounter warning error, solved via:      

```
dash@horse:/media/nvme/jul27_lineage20.0_wTV$ sudo vim hardware/intel/common/libva/Android.bp 
cc_library_shared {
    name: "libva",
......
    cflags: [
        "-Wno-unused-parameter",
        "-Werror",

dash@horse:/media/nvme/jul27_lineage20.0_wTV$ sudo vim external/libva-utils/Android.bp 

cc_library {
    name: "libva_utils_common",

    defaults: ["libva_utils_defaults"],

    srcs: [
        "common/va_display.c",
        "common/va_display_android.cpp",
        "common/va_display_drm.c",
    ],

    export_include_dirs: ["common/"],

    cflags: ["-DHAVE_VA_DRM", "-Wno-unused-parameter"],
```
should install meson:      

```
 pacman -S meson
```
libva version issue:     

```
打开 external/mesa/meson.build（如果是 external/mesa3d/，就打开 external/mesa3d/meson.build）。

跳转到 第 745 行 附近，找到处理 libva 依赖的代码：

Code snippet


# 修改前：
dep_libva = dependency('libva', version : '>= 1.8.0', required : ...)
把 version : '>= 1.8.0' 删掉：

Code snippet


# 修改后：
dep_libva = dependency('libva', required : ...)
```


```
dash@horse:/media/nvme/jul27_lineage20.0_wTV/hardware/waydroid$ sudo vim ./hwcomposer/Android.bp

    cflags: [
        "-DLOG_TAG=\"hwcomposer\"",
        "-Wall",
        "-Wno-error",
        "-Wno-unused-parameter",
    ],

```

继续解决问题:    

```
$  pacman -Ss libxml2
$  pacman -S lib32-libxml2
$ 
$  ln -s /usr/lib/libxml2.so /usr/lib/libxml2.so.2
$  ln -s /usr/lib32/libxml2.so /usr/lib32/libxml2.so.2


$ vim external/mesa/src/intel/compiler/jay/jay_to_binary.c

修改方案
请将第 365 ~ 369 行直接修改为以下内容：

C


   case JAY_OPCODE_DESWIZZLE_ODD: {
      bool hi = simd_offs ? true : jay_deswizzle_odd_src2_hi(I);
      brw_MOV(p, dst,
              byte_offset(to_brw_reg(f, I, simd_offs, 0, false), hi ? 64 : 0));
      break;
   }

nano external/mesa/src/gallium/frontends/va/va_private.h
在 #define VA_DRIVER_INIT_FUNC ... 相关代码下方（或第 460 行之前），加上宏清理：


// 如果定义的 VA_DRIVER_INIT_FUNC 展开后带末尾点号，重新定义它
#ifdef VA_DRIVER_INIT_FUNC
#  undef VA_DRIVER_INIT_FUNC
#endif
#define VA_DRIVER_INIT_FUNC __vaDriverInit_1_0
```

结果:    

```
[root@daa555a53e6e lineage]# ls out/target/product/waydroid_x86_64/*.img -l -h
-rw-r--r-- 1 root root 1.9G Jul 28 02:20 out/target/product/waydroid_x86_64/system.img
-rw-r--r-- 1 root root 635M Jul 28 04:30 out/target/product/waydroid_x86_64/vendor.img

```
### 2. redroid parameters
via:     

```
 sudo docker run --privileged --name fuckddd33 -v /home/dash/ownownjjj:/data -p 5559:5555 -itd ownwaydroid232jul21  ro.hardware.hwcomposer=drm_minigbm
```
