# 20260527
### 1. nuc11 4 screens
via:     

```
 xrandr --output DP1 --mode 1920x1080 --left-of DP2 && xrandr --output DP4 --mode 1920x1080 --below DP1 && xrandr --output DP3 --mode 1920x1080 --below DP2
```
### 2. grep exclude 2 folders

via:     

```
grep -r --exclude-dir=project-objects --exclude-dir=projects "mesa" ./ -r
```
