# 20260302
### 1. hint

```
你现在是古籍数字化重建专家 + 竖排中文排版大师，使用 PaddleOCR-VL 提取结果，从零重建纯文字版 PDF 和 Word (.docx)，目标是让重建版在视觉风格、阅读顺序、行数、界栏等方面尽可能接近原扫描古籍。

本次任务：仅处理 PDF 前30页（严格页码 4 到 33，共30页），作为风格复原测试。

输入 PDF：本目录下的Lunyu.pdf

输出目标：
1. test_rebuilt_lunyu_pages4-33.pdf （使用 reportlab 从零生成）
2. test_lunyu_pages4-33.docx （使用 python-docx）

已确认环境：
- PaddleOCR-VL-1.5-0.9B 通过 vLLM server 调用
- server 地址：http://192.168.1.212:8080/v1
- pipeline 初始化方式：
  pipeline = PaddleOCRVL(
      vl_rec_backend="vllm-server",
      vl_rec_server_url="http://192.168.1.212:8080/v1",
      vl_rec_model_name="PaddleOCR-VL-1.5-0.9B",
  )
- 单张图片调用样例见本目录下的test.py

风格复原强制优先级（必须严格按此顺序权衡，任何冲突都优先前项）：

0. 行数一致性（最高优先级约束）：
   - 重建版必须逐行对应原版行数，同一列的行数误差 ≤1 行
   - 采用“逐行扫描 + 逐行重建”模式：禁止使用自动换行、flowable 或段落自动布局
   - 先通过 OCR 的 y 坐标分布（或 bbox top/bottom 聚类）统计原页每列的视觉行数
   - 重建时每列强制绘制固定行数：缺失行补空行（或适当空白字符），超出行尝试合并短行或微调行距
   - reportlab 绘制使用固定 y 坐标递减（y -= fixed_line_height），fixed_line_height 根据原页平均行距反推

1. 界栏/矩形方框检测与绘制：
   - 如果 PaddleOCR-VL 输出中包含 border / frame / rectangle / ruling / line 类型，或多个文字块的 bbox 形成封闭包围，则必须绘制细黑矩形框
   - 绘制方式：reportlab canvas.rect(x1, y1, x2-x1, y2-y1, fill=0, stroke=1, width=0.6–1.0)
   - 坐标取所有被包围文字块的 min_x/min_y/max_x/max_y，向外扩展 3–6pt 作为边距
   - 如果 OCR 未明确标记，但视觉上明显有界栏（外接矩形明显包围多行文字），也视为需要绘制框
   - 鱼尾、卷首、界线同等处理为矩形或直线

2. 竖排方向与阅读顺序：
   - 整页从右往左排列列（古籍右起左行）
   - 每列从上到下
   - 严格遵循 PaddleOCR-VL 的 reading_order，从最右侧列开始逐列向左
   - 竖排文字使用 canvas.rotate(90) 或自定义坐标变换实现

3. 注释/夹注/双行小字/眉批位置：
   - 双行小字 / 注文 尽量放在正文右侧或行间夹注位置，字号缩小到正文的 60%–75%
   - 眉批放在页面顶部或对应正文上方，字号缩小到 50%–65%
   - 根据 block type 或 bbox 位置判断（右侧/上方/行间）

4. 字号层级：
   - 正文基准 14–18pt
   - 注文/夹注 10–13pt
   - 眉批/小字 8–11pt
   - 根据 bbox 高度或 type 粗估

5. 行距与字距：
   - 行距紧凑（1.05–1.15 倍行高）
   - 字距均匀（0.92–1.0 倍），避免现代宽松间距

6. 分段/章节/圈点：
   - 保留原版明显的段落空行、界栏位置、鱼尾/圈点位置近似
   - 圈点可用小圆点或符号模拟

7. 字体选择：
   - 正文优先 SimSun / STSong（宋体）
   - 注文/注释优先 KaiTi / SimHei（楷体或黑体）
   - 确保繁体/古字/异体字不出现方块

8. 背景：
   - 纯白色（或极浅米色 #FAF9F2），无任何纹理/图像

工作规则：
- 只处理页码 4–33，严禁处理第34页及以后
- 分批处理（建议每批 10 页，共 3 批）
- 每批完成后：
  - 保存中间文件（batch_4-13.pdf / batch_4-13.docx 等）
  - 在 ./test_progress.md 追加：
    - 该批次典型页重建效果描述
    - 乱码/错字/标点错误页统计
    - 竖排阅读顺序是否正确
    - 注释位置是否合理
    - 行数一致性（哪些页误差 >1 行）
    - 是否成功绘制界栏/矩形框（哪些页有框、哪些页漏了）
  - 输出：【测试报告】 + 问题总结 + “是否继续下一批？要调整哪些参数（行距、字号、框线宽、注文位置策略等）？”
- 如果 5 分钟无输入，自动继续下一批
- 允许自主写/改代码、查文档，但 pip install 需建议我执行

现在立即开始前30页风格最大化复原测试：
1. 先检查环境（PDF页数、vLLM连通性、reportlab & python-docx 是否可用），结果写入 ./test_env.md
2. 列出详细拆解清单（至少15–20项，重点包含：原页行数统计逻辑、界栏检测与绘制逻辑、逐行 y 坐标重建逻辑）
3. 从第4页开始，优先生成单页重建函数（reportlab Canvas + 竖排 + 逐行绘制 + 框检测）
4. 处理完第4页后，把核心代码片段贴出来 + 说明：
   - 是否实现了右起竖排
   - 行数是否与原页一致
   - 是否检测并绘制了矩形框（如果该页有框）
   - 注文/夹注位置是否合理
5. 继续处理后续页，直到30页完成或我叫停

启动测试，现在开始，不要等待确认。
```
