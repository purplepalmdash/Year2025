# 20260515
### 1. vulkan configuration
Configuration for `build.prop`:     

```
$ sudo docker cp b60:/vendor/build.prop .
$ sudo cat build.prop | grep vulkan
ro.hwui.use_vulkan=
ro.hardware.vulkan=intel
$ sudo docker cp build.prop  b60:/vendor/build.prop
```

