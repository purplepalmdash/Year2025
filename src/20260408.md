# 20260408
### 1. ext4 creation
Via:        

```
dash@aiBox:~$ diff build_image.py /media/sda/redroid/lineageos21/build/make/tools/releasetools/build_image.py 
127a128,188
> def CheckAndFixExt4InodeCount(in_dir, prop_dict, out_file, target_out, fs_config):
>   """Build ext4 image and ensure final inode count is not smaller than the
>   requested extfs_inode_count.
> 
>   If the actual inode count is smaller than requested, enlarge image_size /
>   partition_size and retry several times.
> 
>   Returns:
>     A tuple of (mkfs_output, fs_dict).
> 
>   Raises:
>     BuildImageError: if the requested inode count still cannot be satisfied.
>   """
>   requested = int(prop_dict.get("extfs_inode_count", "-1"))
> 
>   mkfs_output = BuildImageMkfs(in_dir, prop_dict, out_file, target_out, fs_config)
> 
>   sparse_image = False
>   if "extfs_sparse_flag" in prop_dict and "disable_sparse" not in prop_dict:
>     sparse_image = True
>   fs_dict = GetFilesystemCharacteristics(prop_dict.get("fs_type", ""), out_file, sparse_image)
> 
>   if requested < 0:
>     return mkfs_output, fs_dict
> 
>   max_rounds = 8
>   grow_step = 128 * BYTES_IN_MB
> 
>   actual_inode_count = int(fs_dict.get("Inode count", "0"))
>   round_no = 0
>   while actual_inode_count < requested and round_no < max_rounds:
>     logger.warning(
>         "Ext4 inode target NOT satisfied for %s: requested=%d actual=%d "
>         "(round %d/%d). Increasing image size and retrying.",
>         out_file, requested, actual_inode_count, round_no + 1, max_rounds)
> 
>     try:
>       os.remove(out_file)
>     except OSError:
>       pass
> 
>     new_size = int(prop_dict["image_size"]) + grow_step
>     prop_dict["image_size"] = str(new_size)
>     prop_dict["partition_size"] = str(new_size)
> 
>     mkfs_output = BuildImageMkfs(in_dir, prop_dict, out_file, target_out, fs_config)
>     fs_dict = GetFilesystemCharacteristics(prop_dict.get("fs_type", ""), out_file, sparse_image)
>     actual_inode_count = int(fs_dict.get("Inode count", "0"))
>     round_no += 1
> 
>   if actual_inode_count < requested:
>     raise BuildImageError(
>         "Failed to satisfy extfs_inode_count for {}: requested {}, actual {} "
>         "after {} retries. Please increase partition size or reduce reserved size."
>         .format(out_file, requested, actual_inode_count, max_rounds))
> 
>   logger.info(
>       "Ext4 inode target satisfied for %s: requested=%d actual=%d size=%s",
>       out_file, requested, actual_inode_count, prop_dict.get("image_size"))
>   return mkfs_output, fs_dict
> 
608a670,682
>       #if int(prop_dict["extfs_inode_count"]) >= 0:
>       #  extfs_inode_count = prop_dict["extfs_inode_count"]
>       #  inodes = int(fs_dict.get("Inode count", extfs_inode_count))
>       #  inodes -= int(fs_dict.get("Free inodes", "0"))
>       #  # add .2% margin or 1 inode, whichever is greater
>       #  spare_inodes = inodes * 2 // 1000
>       #  min_spare_inodes = 1
>       #  if spare_inodes < min_spare_inodes:
>       #    spare_inodes = min_spare_inodes
>       #  inodes += spare_inodes
>       #  prop_dict["extfs_inode_count"] = str(inodes)
>       #  logger.info(
>       #      "Allocating %d Inodes for %s.", inodes, out_file)
610,612c684,686
<         extfs_inode_count = prop_dict["extfs_inode_count"]
<         inodes = int(fs_dict.get("Inode count", extfs_inode_count))
<         inodes -= int(fs_dict.get("Free inodes", "0"))
---
>         requested_inodes = int(prop_dict["extfs_inode_count"])
>         used_inodes = int(fs_dict.get("Inode count", requested_inodes))
>         used_inodes -= int(fs_dict.get("Free inodes", "0"))
614c688
<         spare_inodes = inodes * 2 // 1000
---
>         spare_inodes = used_inodes * 2 // 1000
618,619c692,694
<         inodes += spare_inodes
<         prop_dict["extfs_inode_count"] = str(inodes)
---
>         suggested_inodes = used_inodes + spare_inodes
>         final_inodes = max(requested_inodes, suggested_inodes)
>         prop_dict["extfs_inode_count"] = str(final_inodes)
621c696,697
<             "Allocating %d Inodes for %s.", inodes, out_file)
---
>             "Allocating %d Inodes for %s (requested=%d used=%d suggested=%d).",
>             final_inodes, out_file, requested_inodes, used_inodes, suggested_inodes)
648a725,728
> #  if not mkfs_output:
> #    mkfs_output = BuildImageMkfs(
> #        in_dir, prop_dict, out_file, target_out, fs_config)
> 
650,651c730,736
<     mkfs_output = BuildImageMkfs(
<         in_dir, prop_dict, out_file, target_out, fs_config)
---
>     if fs_type.startswith("ext"):
>       mkfs_output, _ = CheckAndFixExt4InodeCount(
>           in_dir, prop_dict, out_file, target_out, fs_config)
>     else:
>       mkfs_output = BuildImageMkfs(
>           in_dir, prop_dict, out_file, target_out, fs_config)
> 
```
