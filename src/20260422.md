# 20260422
### 1. lineageos 14 build
ffmpeg should be changed to specific commit.    

```
cd /media/ssd/lineageos/a14_waydroid/external/ffmpeg

# 先确保没有未提交的修改（如果有，先 stash 或 commit）
git status

# 推荐方式（Git 2.23+ 更现代）
git restore --source=9552caeebc441c762f7f10eb6ce9a455d635fdb6 -- .
```
Then:      

```
export TARGET_USE_MESA=false

vim vendor/intel/media-driver/media_softlet/linux/common/os/i915/include/mos_bufmgr_api.h
Add: #include <errno.h>

vim external/minigbm/gbm_mesa_driver/Android.mk

最外层加：
复制ifeq ($(TARGET_USE_MESA),true)
...原文件内容...
endif

或者至少把定义 libgbm_mesa_wrapper 的那段包起来。
```
Then:     

```
source build/envsetup.sh
export TARGET_USE_MESA=false
lunch lineage_waydroid_x86_64-ap2a-userdebug
source "$HOME/.cargo/env"
time make systemimage -j12
time make vendorimage -j12
```
### 2. redroid16 build
lunch and build step:    

```
lunch redroid_x86_64-aosp_current-userdebug
time m -j12
```
