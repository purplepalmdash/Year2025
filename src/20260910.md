# 20260910
### 1. pi multivision
Configuration:      

```
pi install npm:pi-multivision

$ cat ~/.pi/agent/models.json
{
  "providers": {
"ollama": {
      "baseUrl": "http://127.0.0.1:8021/v1",
      "api": "openai-completions",
"requireHttps": "false",
      "apiKey": "none",
      "models": [
        {
          "id": "qwen354b-256k:latest",
          "contextWindow": 131072,
          "name": "Qwen354B-256k(Ollama)",
	"input": ["text", "image"]
        }
      ]
    },
    "llama-cpp": {
      "baseUrl": "http://192.168.1.165:8010/v1",
      "api": "openai-completions",
      "apiKey": "none",
      "models": [
        {
          "id": "Qwen3.8-27B-Q4_K_M.gguf",
          "contextWindow": 100000,
	 "input": ["text"],
          "name": "Qwen3.8-27B(remote)"
        }
      ]
    }
  }
}
$ cat ~/.pi/agent/pi-multivision.env 
VISION_MODEL_3_NAME=qwen354b
VISION_MODEL_3_URL=http://192.168.1.165:8021/v1
VISION_MODEL_3_MODEL=qwen354b-256k:latest
VISION_MODEL_3_KEY="none"
```
Disable thinking mode in ollama:        

```
curl http://localhost:11434/api/generate -d '{  "model": "qwen354b-256k","think":false}'
```
Modification for 100K:       

```
 cat startb60_100K.sh
NAME=llamacpp-sycl-qwen38
IMAGE=llama.cpp:sycl-f16-b10433
MODEL_DIR=/home/dash/intelxe/models
PORT=8010   # NOT 8003 — headroom-proxy (Anthropic upstream) owns 8003

docker run -d --name "$NAME" \
  --restart unless-stopped \
  --memory=22g --memory-swap=24g \
  --device /dev/dri \
  --group-add "$(getent group render | cut -d: -f3)" \
  --group-add "$(getent group video  | cut -d: -f3)" \
  -v "$MODEL_DIR":/models:ro \
  -p "0.0.0.0:${PORT}:8000" \
  -e ONEAPI_DEVICE_SELECTOR=level_zero:0 \
  "$IMAGE" \
  -m /models/Qwen3.8-27B-Q4_K_M.gguf \
  --model-draft /models/mtp-Qwen3.8-27B-Q4_0.gguf \
  --spec-type draft-mtp --spec-draft-n-max 3 \
  -ngl 99 \
  -c 100000 \
  --cache-type-k q4_0 --cache-type-v q4_0 \
  --parallel 1 \
  --host 0.0.0.0 --port 8000 \
  --metrics \
  -fa on -ub 2048 -b 2048 \
  --jinja --reasoning on \
  --temp 1.0 --top-p 0.95 --top-k 20 --min-p 0.0 \
  --repeat-penalty 1.0
```
