# 20260407
### 1. reback modification
Steps:     

```
cd /media/big/lineageos/lineageos22

# 1. 强制恢复这几个关键文件到上游原始状态
git -C device/virt/virt-common checkout -- configs/scripts/create_partition_table.sh
git -C device/virt/virt-common checkout -- build/tasks/01-diskimage.mk
git -C device/virt/virt-common checkout -- services/preinstall_check/preinstall_check.cpp
git -C device/virt/virt-common checkout -- BoardConfigVirtCommon.mk

git -C device/virt/virtio_x86_64 checkout -- BoardConfig.mk

# 2. 彻底删除编译输出（非常重要）
rm -rf out/target/product/virtio_x86_64/

# 3. 重新生成磁盘镜像，使用官方默认配置
make -j$(nproc) diskimage-vda
```
Verification:      

```
git -C device/virt/virt-common status --short
```
