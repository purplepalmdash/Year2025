# 20260810
### 1. logcat output
Step:       

```
adb logcat -c
adb logcat -d > vlc_play.log
```
### 2. redroid(gralloc)
Build:      

```
 lunch redroid_x86_64_only-bp2a-userdebug
 m gralloc.redroid
 ls out/target/product/redroid_x86_64_only/obj/SHARED_LIBRARIES/gralloc.redroid_intermediates/gralloc.redroid.so
```

